# SentinelNet 哨兵网络 — 校园网加密流量入侵检测系统依赖清单
# 安装: pip install -r requirements.txt
# 约定：与 pyproject.toml [project].dependencies / [project.optional-dependencies] 双份同步

# ── FastAPI 生态（ADR-0001）────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy>=2.0
alembic>=1.13
sse-starlette>=2.0
slowapi>=0.1.9

# ── 安全响应头（ADR-0001 §1.1 / §6 #3，T1.11 ③）──────────
secure>=0.3.0

# ── 既有 Web 监控面板（Flask，旧版保留）─────────────────────
flask>=3.0
flask-talisman>=1.0
flask-login>=0.6
flask-cors>=4.0
flask-limiter>=3.0
flask-wtf>=1.2
waitress>=2.1
flasgger>=0.9

# ── 抓包与流量分析 ────────────────────────────────────────
scapy>=2.5

# ── 数据处理与模型训练 ────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
imbalanced-learn>=0.11

# ── 可视化 ────────────────────────────────────────────────
matplotlib>=3.7

# ── 运维增强 ──────────────────────────────────────────────
python-dotenv>=1.0
psutil>=5.9
tqdm>=4.60

# ── 开发期工具（ADR-0001 §1.1，pip install -r requirements.txt 不含，需 -e .[dev]）──
# pytest-regressions>=0.1    # T0.2/T0.3: golden file 录制与比对
# pytest-alembic>=0.1        # T1.10: 迁移链自动化测试
# oasdiff 为 Go 单二进制，不在此处管理
