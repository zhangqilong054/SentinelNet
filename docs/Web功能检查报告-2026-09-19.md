# Web 功能检查报告（2026-09-19 第二轮）

- **检查时间**：2026-09-19 13:08–13:12
- **基线 commit**：`8ec3edb`（fix(tests): 修复测试环境 Settings 单例缓存导致的 secret_key 断言错误并修正 uvicorn 启动入口）
- **工作区状态**：干净（`git status --short` 无输出，`git diff` 为空）
- **解释器**：`C:\Users\18551\anaconda3\python.exe`（Python 3.13.9，win32）
- **相对上轮（6b99097）的变化**：仅新增 1 个提交，改动 `tests/conftest.py`（+7，即上轮遗留 P0-1「DEBUG import 期 setdefault」正式入库）、`src/campus_ids/web_new/app.py`（uvicorn 启动入口修正）与操作手册。无业务逻辑变更。

## 结论：Web 功能整体健康 ✅

| 检查项 | 结果 | 证据（可复现命令见下） |
|---|---|---|
| 全量测试 | ✅ **781 passed / 0 failed**，118.6s，退出码 0 | `$PY -m pytest tests/ -q --basetemp=... -p no:cacheprovider` |
| 覆盖率（新模块口径） | ✅ TOTAL **94%**，最低 `payload.py` **74%**（≥70% 阈值） | `--cov=campus_ids.services --cov=campus_ids.web_new.api --cov=campus_ids.runtime.events --cov=campus_ids.web_new.pages --cov=campus_ids.web_new.app --cov-report=term-missing` |
| T2 验收主力探针 | ✅ **39 通过 / 0 未通过**（退出码 0） | `$PY scripts/probe_t2_acceptance.py` |
| 测试隔离体检 | ✅ **9/9 被重定向**（本轮实测 24 处绑定改写） | `$PY scripts/probe_test_isolation.py` |
| 契约回放门禁 | ✅ 未解释差异 **0**（真实请求 7 / 页面 6 / 存在性 28），退出码 0 | `$PY scripts/replay_contract.py` |
| 规格门禁（5 层） | ✅ 未解释丢失 **0**（34 旧路径全保留/已解释），退出码 0 | `$PY scripts/replay_contract.py --schema-only` |
| 真机 uvicorn 冒烟（默认模式） | ✅ 见下方明细 | 隔离 data_dir + `uvicorn ... --factory --port 8898` |
| 真机 uvicorn 冒烟（SPA 模式） | ✅ `CAMPUS_IDS_FRONTEND=new` 下 SPA fallback 生效 | 同上，端口 8899 |
| 产物零污染 | ✅ 6/6 md5 **OK** | 跑前记 md5 基线，跑后 `md5sum -c` 全过 |

## 真机冒烟明细（真实 uvicorn + curl --noproxy）

**默认模式（legacy SSR 页面）**：

| 路径 | 状态 | Content-Type |
|---|---|---|
| `/` | 200 | text/html（含 `csrf-token` meta） |
| `/login` | 200 | text/html（含 `csrf_token` 隐藏域） |
| `/change-password` | 200 | text/html |
| `/logout` | **405**（刻意，防跨站 GET 登出） | — |
| `/api/health` | 200 | application/json |
| `/docs` / `/openapi.json` | 200 | html / json |
| `/static/css/app.css` | 200 | text/css |
| `/static/js/app.js` | 200 | application/javascript |

安全响应头 4/4 命中：HSTS、X-Content-Type-Options、X-Frame-Options、CSP。

**SPA 模式（`CAMPUS_IDS_FRONTEND=new`）**：`/` 与 `/tasks` 均 200 text/html，`/` 内容含 dist 标记 `id="app"`（确认托管的是 `frontend/dist` 产物而非 legacy Jinja 回退）。

## 覆盖率明细（每模块口径，阈值 ≥70%）

```
runtime/events.py            97%   web_new/api/admin.py     94%
services/*（7 个文件）       100%  web_new/api/alerts.py    85%
web_new/api/models.py       100%   web_new/api/system.py    86%
web_new/api/auth_routes.py   97%   web_new/api/payload.py   74%（最低）
web_new/api/stream.py        98%   web_new/app.py           81%
web_new/pages.py             94%   TOTAL                    94%
```

## 环境坑记录（本轮实测）

1. **WorkBuddy 会话 shell 的 PATH 丢失**：`head/tail/mkdir/ls` 等基础工具全部 `command not found`，须在每条命令前手动 `export PATH="/c/Users/18551/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:...usr/bin:$PATH"`。
2. **后台 uvicorn 不跨命令存活**：Bash 工具每次调用结束会回收后台子进程 → 冒烟必须「起服务 + curl 全清单 + kill」写在**同一条命令**里。
3. pytest 全量 118s（比上轮 386s 记录快很多，机器负载差异；数字口径不变）。

## 局限与未检查项

- **浏览器端 UI 交互未验证**（仍只到 curl/HTTP 层）；Playwright e2e（`npm run e2e`，9 用例）上轮 9/9 通过，本轮未复跑。
- **SSE 未在真机单独 curl 验帧**：由全量套件中的 `tests/test_sse_live.py`（真实 uvicorn + 裸 socket）覆盖，本轮随全量跑通过。
- 真实 Npcap 抓包 / 训练端到端 / Docker 构建未测（会覆盖产物或需专用环境）。
- T2.19 的 10 分钟真机并发未跑（缺带网卡的机器）。
- 未跑 ruff/mypy。

## 遗留事项（与上轮一致，无新增）

1. `ModelService.delete_model()` 不更新 `latest.json`/`best.json` → 删除被指针指向的模型会留悬挂指针（不崩但 UI 显示不存在的「最佳模型」）。
2. 真实数据集重训闭环（涉及覆盖 `model.pkl` 等产物，需确认后执行）。
3. 既有库 `created_at` 历史废值未做数据迁移。
4. SPA 模式下 `GET /logout` 由 fallback 返回 200（legacy 口径 405，登出走 `POST /api/logout`，安全无恙，契约快照是 legacy 口径）。

---

# 遗留事项修复轮（同日 13:15–14:00，用户确认后执行）

以上遗留 1/2/3 已全部闭环，局限项中的 e2e / SSE 真机 / ruff / mypy 已补齐。修复后全量回归 **789 passed / 0 failed**（exit 0，119s），覆盖率 TOTAL 94%（最低 `payload.py` 74%）。

## ✅ 修复 1：delete_model 悬挂指针

`web_new/api/models.py`：新增 `_recompute_pointers()` / `_write_pointer_files()` / `_atomic_write_json()`。删除 run 后按 `model/train.py` 自身规则重算：

- `latest.json` = 剩余 run 中 `created_at` 最大者；`best.json` = `f1_score` 最大者（无带分 run 则删指针）；registry 的 `is_best` 与 best 对齐（至多 1 个 True）；
- 注册表写失败（500）时不动指针文件，不留半更新状态；
- 删空后两个指针文件一并移除。

新增 6 个用例（`tests/test_models_api.py::TestDeleteModelPointerRecompute`，期望值来自 train.py 规则这一外部真相源）。

## ✅ 修复 2：created_at 历史废值

- 真实库只读实测：`alerts`(350 行) / `traffic_history`(1235 行) / `users`(1 行) **均为 0 废值**（当前库建于修复之后）；
- 仍补**防御性迁移** `migrations/versions/0002_repair_created_at_garbage.py`：alerts/traffic_history 废值行复用业务时间列 `time`（`COALESCE(NULLIF(time,''), CURRENT_TIMESTAMP)`），users 用迁移执行时刻；幂等，只命中字面量行；downgrade 为 no-op；
- `tests/test_migrations.py` 新增修复行为 + 幂等 2 个用例；`test_initial_revision_has_no_parent` 由「取 head」改为「按 0001 判断」（多版本后会误报）。

## ✅ 闭环 3：真实数据集重训（用户确认完整模式）

- 命令：`train(dataset_path=data/, dataset_type='auto', quick=False)`，17m54s，退出码 0；
- **`data_source=dataset:data`，16 万样本**，best_model=RandomForest（accuracy 0.9979 / f1 0.9979 / FPR 0.32%），DualFusion 0.9979，跨数据集评估 8 组全 ≥0.977；
- 新 run `20260919_133750_xosxdw`（15 特征）；`latest` → 新 run，`best` → 旧 `20260912_212239_7nt5pv`（f1 0.9998 更高，按分保留），`is_best` 唯一对齐，指针零悬挂，全部可 `joblib.load`；
- 重训前根产物已备项目外副本：`C:/Users/18551/AppData/Local/Temp/sn_artifact_backup_20260919/`（13:09 md5 基线）。

## 静态检查（原局限项）

- **ruff 0.15.1：77 → 0**。52 处自动修复；🔴 **抓出 1 个真实 bug（F821）**：`evaluation.py:252` 引用不存在的 `cv_f1`，NameError 被下方 `except` 吞掉 → 每轮 CV 误报「交叉验证失败」且丢日志，已改为 `cv_f1_mean/cv_f1_std`（重训日志中 CV-F1 行正常输出即修复生效证据）；手修 E741×3 / E731×1 / F841×6；4 处刻意副作用导入加 noqa（conftest / test_skeleton×2 / probe_t1）；⚠️ `ruff --fix` 曾误删 `train.py` 的 `TRAFFIC_CSV` 导入（隔离夹具重定向锚点），已恢复并标注 noqa，隔离探针复验 9/9；剩 9 处刻意保留（E402 import 顺序语义、detector.py 冻结模块 E731）用 `pyproject.toml` per-file-ignores 固化。
- **mypy 1.17.1 基线**：web/runtime/services 范围 51 errors / 13 文件（scapy/matplotlib 无 stub、联合类型收窄等标注债），记为基线未修，后续可分批收敛。

## 真机验证（原局限项）

- **Playwright e2e 复跑：9/9 passed**（43.8s，含 SPA 五路由、SSE 已连接、限流 429、多标签 CSRF、真杀后端断线重连、5 页视觉基线）。
- **SSE 真机 curl 验帧**：`/api/stream?topics=traffic|alert` 首帧 `event: connected` 正常；未知 topic → 400；业务帧由 `test_sse_live.py`（真实 uvicorn + 裸 socket + 生产者）覆盖且本轮全绿。

## 更新后的遗留

1. ~~delete_model 悬挂指针~~ ✅ 已修复；~~created_at 废值迁移~~ ✅ 已收敛；~~真实数据集重训~~ ✅ 已闭环。
2. SPA 模式 `GET /logout` fallback 200（契约快照 legacy 口径，安全无恙）——维持原状。
3. T2.19 十分钟真机并发 / 真实 Npcap 抓包 / Docker 构建（本轮未触发，需专用环境）。
4. mypy 51 处标注债（基线已记录）。

（本报告及修复代码已随本次提交入库（hash 见 `git log`，提交信息：fix: 修复delete_model悬挂指针…）；`.mypy_cache/` 已加入 .gitignore。）
