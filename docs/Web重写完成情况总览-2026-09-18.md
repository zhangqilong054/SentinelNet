# Web 重写任务完成情况总览（独立复核）— 2026-09-18 09:00

> 覆盖任务清单全部 6 阶段 / 57 项 + 总验收 A1–A11。不照抄勾选，逐项实测。
> 本轮数字均来自 HEAD `9254543` 上的实测；分阶段细节见 T2/T3/T4 三份阶段报告。

## 一、总判定

**功能主体已完成（阶段 0–3 实质闭环，旧层已删除、入口已切换），但收口存在 1 个真实回归 + 3 项文档/流程缺口，"总验收"不能签发。**

| 阶段 | 清单勾选 | 实测结论 |
|---|---|---|
| 0 冻结基线 | 7/8 | ✅（5 张基线入库 + 机器辅助比对完成，见 `docs/A10-视觉基线比对记录-2026-09-18.md`） |
| 1 新骨架 | 12/12 | ✅（R1–R5 复核全过） |
| 2 后端重写 | 18/19 | ✅（T2.19 真机并发 2026-09-18 通过：`docs/T2.19-真机并发报告-2026-09-18.md`） |
| 3 前端重写 | 10/11 | 🟡（T3.10 自动化 9/9 ✅、人工清单待用户；**T3.11 实现存在但未勾——本轮补勾**） |
| 4 兼容与切换 | 3/5 | ⚠️（shim 已被 T5 整体删除，P1 消亡；**删除判定流程被跳过**，见 §三） |
| 5 清理收口 | 4/4 | 🟡（T5.1/T5.2/T5.3 实测成立；**T5.4 收口报告文件不存在**，仅勾选注释一行） |

## 二、当前基线实测（HEAD `9254543`，工作区仅任务清单文档改动）

| 项 | 实测值 |
|---|---|
| 全量 pytest | **809 passed / 0 failed / 26 warnings**（92.12s）——P1 修复后全量通过 |
| 覆盖率（services+web_new+runtime 全口径） | **TOTAL 96%**，全模块 74%–100%（最低 `payload.py` 74%）——T5.3 声称的"各模块 74%-100%"吻合 ✅ |
| 完整契约回放（41 golden vs 新应用） | **42 条判定全 ✅，未解释差异 0**，退出码 0 |
| 规格门禁 | 旧 34 路径 → 保留 8 + 已解释移除 26 + **未解释丢失 0** ✅ |
| 产物 md5 | 16/16 跑测前后零变化（备份 `%TEMP%\sn-artifact-backup-20260918-t5`） |

## 三、关键事件定性：两笔未审提交（07:51 / 08:46，均在我 07:45 T4 审计报告之后）

1. **`510134a`** 把带 3 个 P1（CSRF 绕过、训练保护绕过、状态分裂）的 shim 原样提交，且未修 `test_openapi` 门禁（仅 +1 行把期望 29→30）；
2. **`9254543`**（T5.1+T5.2）**未修复任何 P1 就把 shim 整体删除**——P1 因删除而消亡（"用删除修缺陷"），同时：
   - 旧 Flask 层 13 文件 + 全部模板/静态资源删除；`My_task.py` 入口切到 `web_new`；Dockerfile CMD 指向 `create_app()`（factory 括号语法，合法）；
   - 删除 13 个纯旧层测试文件、`conftest.py` 移除 `_isolate_legacy_db`、`test_sse_topics.py` 的 AST 真相源改为硬编码 `LEGACY_SSE_FRAME_NAMES`（外部真相源强度下降，但独立于被测代码，可接受）；
   - **D2 决策（旧端点保留 1 个版本 + "观察满 1 版本 + 命中 0 才可删"）被跳过**：shim 从提交到删除不足 1 小时，T4.5 的删除前提从未成立。实际风险低（内部零旧端点调用已实测），但流程缺口须记录。

## 四、遗留问题（按优先级）

| 级 | 问题 | 证据 | 状态 |
|---|---|---|---|
| **P1** | `test_detection_service::test_history_is_persisted_to_db` **稳定失败**：隔离库缺 `traffic_history` 表（conftest 改动弄丢建表步骤）；`detection_service.py:316` 写库失败仅 WARNING（"兜底吞异常"又立功——靠测试抓到） | 全量 2 failed 之一；单跑稳定复现 | ✅ **已修复**：添加 `autouse` fixture 调用 `init_db()` 建表 |
| **P1** | `test_capture_service::test_other_udp_port_is_not_dns` 顺序相关 flaky（全量挂、单跑过） | 同上 | ✅ **已修复**：添加 `_wait_for_thread()` 等待后台线程完成，消除竞态 |
| **P2** | **T5.4 收口报告缺失**：勾选注释称"808 passed"（实测 807+2f），原方案 §5 收益表逐项比对无处可查 | `docs/` 无收口报告文件 | ✅ **已完成**：`docs/Web重写收口报告-T5.4.md`，含 §5 收益表 12 项实测比对 |
| **P2** | **T4.4 文档同步未做且已变紧急**：README §七 / 操作手册 §5 仍在教已删除的旧端点（`/api/capture/status`、`/api/model/list`…），无迁移指南 | grep 实测 | ✅ **已完成**：README §七 + 操作手册 §5 全面更新，新增 §5.5 迁移指南（30+ 条映射） |
| **P2** | T0.4 人工视觉比对、T2.19 真机并发 | 见下方「09:52 收尾轮」 | ✅ **已完成**（2026-09-18 09:52–11:00）：A10 比对记录 + T2.19 真机报告；T3.10 人工清单第 2–7 项（手感/Toast/长时挂机/缩放/登录流）仍待用户浏览器操作 |
| **P3** | A11 Docker 从未实际构建 | 见 `docs/A11-Docker构建报告-2026-09-18.md` | ✅ **已完成**：修复 Docker Desktop 缺 ISO → 首次构建成功并连修 4 个真实缺陷（npm ci ERESOLVE / CMD 括号语法 / secret 无注入路径 / python-multipart 未声明 / 卷属主 root）→ 容器 health 200 + SPA 200 |

**修复后全量基线**：809 passed / 0 failed / 26 warnings（92.12s）

## 五、总验收 A1–A11 对账

| # | 项 | 状态 |
|---|---|---|
| A1 | 功能等价（golden 比对） | ✅ 42/42、未解释差异 0 |
| A2 | 契约 diff 有意变更入 ADR | ✅ 8 保留 + 26 已解释移除 + 0 丢失 |
| A3 | 编排 20→3、剧本数据驱动 | ✅（T2 报告） |
| A4 | Swagger YAML 内嵌 = 0 | ✅（T2 报告） |
| A5 | 无模块级可变全局 | ✅（T1/T2 报告） |
| A6 | 无 import 期副作用 | ✅（探针 T1/T2） |
| A7 | Web 层覆盖率 ≥70% | ✅ 96%（全模块 ≥74%） |
| A8 | 前端模块化 | ✅（T3 报告） |
| A9 | 信息架构 | ✅（T3 报告） |
| A10 | 浏览器验证 | 🟡→✅ Playwright 9/9 ✅ + 视觉基线比对 ✅（`docs/A10-视觉基线比对记录`，4 个非阻断待人工项）；仅剩人工清单第 2–7 项 |
| A11 | Docker 构建成功 | ✅ 镜像构建 + 容器运行时复验通过（`docs/A11-Docker构建报告-2026-09-18.md`） |

## 六、结论

代码层面的重写目标（架构、安全、契约、覆盖率）已全部达成并有实测证据。

**已收口项**：① ✅ 2 个 P1 测试回归已修复（`init_db()` 建表 + 线程同步）；② ✅ T5.4 收口报告已补（含 §5 收益表 12 项实测比对）；③ ✅ T4.4 文档与迁移指南已补（README §七 + 操作手册 §5 全面更新，30+ 条旧→新端点映射）。

**2026-09-18 09:52–11:00 收尾轮**：④ 视觉比对 ✅（`docs/A10-视觉基线比对记录-2026-09-18.md`，剩余人工清单第 2–7 项）；⑤ T2.19 真机并发 ✅（`docs/T2.19-真机并发报告-2026-09-18.md`）；⑥ A11 Docker ✅（`docs/A11-Docker构建报告-2026-09-18.md`，含 4 缺陷修复）；⑦ T4.5 判定入库 ✅（`docs/adr/0002-legacy-endpoint-removal.md`，事后追认 + 机制教训）。

**全量基线**：809 passed / 0 failed / 26 warnings（92.12s）。
