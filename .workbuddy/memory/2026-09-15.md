# 2026-09-15

## SentinelNet 项目全面检查

对 D:\18551\AiC\SentinelNet（校园网加密流量入侵检测系统）做了一次完整检查，产出报告：
`docs/项目检查报告-2026-09-15.md`

### 环境关键信息（后续复用）
- 可用解释器：`C:\Users\18551\anaconda3\python.exe`（Python 3.13.9，已装 campus_ids + 全部依赖，pytest 可用）
  - workbuddy 自带 python 3.13.12 / msys64 python 3.12.11 **均未安装项目依赖**，不要用
- 应用入口：`python My_task.py app`（只接受 app/无参数，已无 CLI 子命令）
- Web 层没有 `create_app()`，模块级 `from campus_ids.web.app import app` 才是入口对象
- 测试：`python -m pytest tests/ -q` → 144 passed

### 查出的主要问题
1. **P0 安全**：`GET /api/cleanup`（bp_admin.py:114）会 DELETE alerts/traffic_history。
   未声明 methods 默认 GET，而 Flask-WTF CSRFProtect 只保护 POST/PUT/PATCH/DELETE，
   且默认 `CAMPUS_IDS_API_TOKEN` 为空 → 认证关闭。实测 GET 200 无需认证/CSRF，POST 返回 405。
   `GET /api/save` 同类问题（写 CSV）。
2. **P1 可信度**：README 称 model.pkl 为 CICIDS2017（Attack F1=0.9998），
   实际 best.json → run 20260914_123225_6i4cf6，其 metrics.json `data_source: synthetic_demo`，
   `evaluation_report.txt` 同为 synthetic_demo。该模型 17 维，当前 FEATURE_NAMES 18 维（差 max_pkt_len）。
3. **P1 配置**：`.env.example` 漏 12 个 config.py 支持的变量；app.py:81 secret key 硬编码兜底且无告警。
4. **P2 文档漂移**：README 测试数 109/7 文件 → 实际 144/12 文件；API 表缺 3 个 detector 端点；
   cleanup 方法标注错误；结构树缺 limiter.py、change_password.html；
   `docs/checklist.md` 勾选"models 表替代 registry.json"，实际 SQLite models 表 0 行、仍在用 registry.json。
5. **P3 卫生**：`docs/out.txt`（55KB 终端倾倒）未跟踪，建议删除。

### 排除的误报（重要，避免下次重踩）
- `model.pkl` / `scaler.pkl` 用裸 `pickle.load` 会报 `invalid load key \x05`，**不是文件损坏**——
  joblib 用 protocol 5 + 带外缓冲，必须用 `joblib.load`。9 个 pkl 全部加载正常。
- `ENHANCED_FEATURE_COLUMNS` 定义在 `model/data_loader.py`（= list(FEATURE_NAMES)），
  不在 `capture/enhanced_features.py`（那里叫 `FEATURE_NAMES`）。train.py 从 data_loader 导入，无问题。
- `dual_detector.py:298` 用 `align_features()` 按模型自身 feature_columns 对齐，15/17/18 维模型都能推理，设计正确。

### 未执行的操作
- 未运行训练流程（会覆盖 model.pkl / evaluation_report.txt / confusion_matrix.png 等用户产物）
- 未修改任何源码（用户只要求"检查"）

---

## 23:42 复核：报告完成度审查（报告已升级 v2）

用户要求"检查报告完成度"。发现 v1 报告**已过时**——23:26–23:38（即 v1 出具后）代码与文档被修改：
`app.py`、`bp_admin.py`、`README.md`、`.env.example`、`docs/checklist.md`、`docs/操作手册.md`（+108/−39）。
v1 的 12 项问题中 **11 项已闭环**（cleanup/save 改 POST、secret key 告警、README 模型声明、测试数 144、
API 表补 detector 端点、checklist models 表改回 [ ]、docs/out.txt 删除）；仅"用 CICIDS2017 重训"未做。
基线：`HEAD = 7021eb2`，上述修复**仍未 commit**。

### v2 新增的 2 个真缺陷（v1 遗漏）
1. **N-1 回归（🔴）**：`controls.js:385` 仍 `await SN.apiGet('/api/save')`，而后端 `/api/save` 已改 POST-only
   → 面板「保存 CSV」按钮 405 失效。修复：改 `SN.apiPost('/api/save')`（api.js:104 已存在）。
   已写脚本对 28 处前端调用 × 34 个后端路由做全量方法比对，**仅此 1 处错配**。
2. **N-2 配置静默失效（🟠）**：`.env.example:53` 写 `CAMPUS_IDS_LATERAL_MOVEMENT_THRESHOLD`，
   而 `config.py:64` 读 `CAMPUS_IDS_LATERAL_THRESHOLD` —— 变量名不符，用户按示例设置**不生效且无告警**。
   README/操作手册写的是正确名，只有 .env.example 错。

### v2 补做的关键维度：真实覆盖率
`pytest-cov --cov-branch`（装到独立目录，未污染环境）→ **TOTAL 34%**（3501 语句 / 2211 未覆盖）。
- 优秀：`detector/detector.py` 100%、`config.py` 92%、`attack_sim_state.py` 85%
- 薄弱：`model/evaluation.py` **5%**、`logical_config`（logging_config.py）**0%**、`model/train.py` 15%、
  `model/data_loader.py` 17%、`web/bp_admin.py` 21%、`web/helpers.py` 23%
- 即「训练→评估链路 + Web 蓝图层 + 日志系统」几乎无测试保护，README 的「144 个测试」易高估质量

### 方法论教训（下次做项目审查必做）
- 报告**必须标注基线 commit + 工作区脏状态**，否则结论随时失效（本次就是因此过时）
- 环境变量核对要**双向 + 校验变量名相等**，只查"缺失"会漏掉改名错误
- 前端↔后端接口要**交叉比对 method**，改后端方法必须同步搜前端调用方
- 审查报告要有「未检查项/局限」章节，否则读者误以为全量覆盖
- 未做：Docker 实际构建、浏览器 UI 操作、真实抓包、训练端到端、ruff/mypy 静态检查
